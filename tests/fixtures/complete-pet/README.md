# Complete Pet Fixture

Tracked no-network fixture metadata for the complete v2 package gate. Tests generate the deterministic atlas pixels from `atlas.json` so the repository does not carry a large derived bitmap.
